name: sunny
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.12.2
  intl: ^0.17.0
  geolocator: '^5.1.1'
  geocoding: ^2.0.0
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - asset/image/sunny.png
    - asset/image/wind.png
    - asset/image/humidity.png
    - asset/image/thermometer.png
